using UnityEngine;
using System.Collections;

public class KeyLocationLeaderEndpoint : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//gameObject.SetActive(false);
		GetComponent<Renderer>().enabled = false;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
